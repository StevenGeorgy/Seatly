Re-verify Hey Cenaiva 20 capabilities against Mark's account on localhost:5173. Drive Chrome MCP via find/left_click/type/screenshot. Allowed dev-bridge: open(), close(), setTextMode(true), getState(), getSpoken(). NOT allowed: send(). KEEP ITERATING UNTIL ALL 20 ARE ✓ CLEAN — no iteration cap. EXPANDED LATITUDE: (a) DB migrations ALLOWED via supabase MCP on exbjodmnpdiayfzrdyux for event-day shift overrides, test-fixture date moves, product fixes. (b) FILE SCOPE — modify anything under apps/web/src/ and supabase/ EXCEPT useCenaivaWakeWord.ts. (c) LLM PROMPT REWRITES expected when LLM omits filters. (d) TEST DATA EDITS allowed (move Wagyu event from Sunday to Saturday, add Cafe restaurants). (e) NEW LIBRARIES OK (Metaphone, accent strippers) via npm: imports. (f) PRAGMATIC ✓ — cap passes when behavior is correct from real diner's POV; if specific phrasing fails but canonical passes, document variant + count ✓. ## 20 caps. 1-17 from CLAUDE.md "Headline state (2026-05-13)": book/modify/cancel + 3 event-linked (Wagyu Masterclass, Beaune Burgundy, Live Music Friday — each event_id non-null in DB) + promos/events/hours + deposit hand-off (party≥8 $80 Mark Testing) + pre-order hand-off + joke + modify regex disambig (v305) + cancel gating (v306) + confirmation routing + casual handler (v292) + missing-field phrasing (v290) + colloquial party words + event-theme filter (v291) + modify-confirm fallback (v293). Cap 18 — Mid-flow correction: "actually nevermind", "wait change to 8 people", "no make it Friday", "scratch that", "make it 7pm not 8pm". Reset right field or ask clarification, NEVER keep stale, NEVER crash. Cap 19 — Recommendation quality + business_type aliases. Prompts: "date night", "cheap eats near me", "best steakhouse", "patio spot", "anniversary place", "lunch under $30", "find me a cafe", "coffee shop", "any steakhouses", "cocktail bar", "lounges open", "gastropub", "fine dining tomorrow", "casual dining", "STEAKHOUSE", "food truck"(unknown→broader not zero), "italian cafe"(empty graceful). Real DB restaurant, marker_restaurant_ids real ids, ≥1 card rendered. Cap 20 — Misheard: "bone burgundy"→Beaune Burgundy, "jacob's co"→Jacobs&Co., "baton rouge"→Bâton Rouge (not Louisiana), "wagu wensday"→Wagyu Wednesday. Add accent normalize + phonetic similarity (Metaphone). ## Pass criteria. (1) spokenText correct. (2) Bookings: DB row verified (reserved_at, party_size, event_id when event-linked, restaurant_id, confirmation_code). (3) Rec: marker_restaurant_ids real. (4) Correction: booking_state updated + cleared contradictions. (5) Screenshot. (6) Cap 19: ≥1 card rendered via find+screenshot. ## Loop. test→diagnose→fix(code|migration|prompt|data)→type-check→deploy→retest. Type-check: `npx tsc --noEmit -p apps/web/tsconfig.app.json` (only pre-existing ui_actions error). Deploy: `supabase functions deploy cenaiva-orchestrate --project-ref exbjodmnpdiayfzrdyux`. Migrations: kebab-case timestamp to supabase/migrations/. ## Cleanup. Mark test rezzes special_request='GOAL-TEST <cap#>'. End: DELETE FROM public.reservations WHERE user_profile_id='de3fbe5e-0c7f-4d35-93f5-eaa2e0910209' AND special_request LIKE 'GOAL-TEST%'. ## Deliverable. CENAIVA_20CAP_REPORT_2026-05-14.md at repo root: table(cap#|name|✓/⚠/✗|retries|notes); fixes(file:line + before/after); migrations(name+purpose); prompt edits(lines+reason); rephrased transcripts(original|rephrased|why); totals; draft CLAUDE.md headline v310+ block. Success = 20/20 ✓ clean only. Begin iter 1: read CLAUDE.md headline state, plan order (easy caps first, hardest last), real UI automation.
