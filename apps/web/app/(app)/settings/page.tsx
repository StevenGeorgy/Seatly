import { PlaceholderPage } from "@/components/placeholders/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Restaurant profile, booking rules, deposit config, loyalty config, notification preferences."
    />
  );
}
