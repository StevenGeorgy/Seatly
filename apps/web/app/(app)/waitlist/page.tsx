import { PlaceholderPage } from "@/components/placeholders/PlaceholderPage";

export default function WaitlistPage() {
  return (
    <PlaceholderPage
      title="Waitlist"
      description="Live queue of walk-in guests waiting for a table."
    />
  );
}
