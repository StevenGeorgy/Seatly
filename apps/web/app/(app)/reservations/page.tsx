import { PlaceholderPage } from "@/components/placeholders/PlaceholderPage";

export default function ReservationsPage() {
  return (
    <PlaceholderPage
      title="Today's Reservations"
      description="Timeline view of all bookings today sorted by time."
    />
  );
}
