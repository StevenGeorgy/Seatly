import { PlaceholderPage } from "@/components/placeholders/PlaceholderPage";

export default function NewReservationPage() {
  return (
    <PlaceholderPage
      title="New Reservation"
      description="Creates a reservation when a customer calls. Fields: name, phone, party size, date, time, occasion, special request."
    />
  );
}
